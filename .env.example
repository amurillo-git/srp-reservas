# Copiar este archivo como .env y completar los valores reales.
# .env nunca debe subirse al repositorio (ver .gitignore).

# Cadena de conexion a la base de datos PostgreSQL administrada
# (por ejemplo, Supabase o Neon.tech) que usara Prisma.
DATABASE_URL="postgresql://usuario:password@host:5432/srp_reservas?schema=public"

# Puerto donde escuchara el servidor Express.
PORT=3000

# Entorno de ejecucion: development | test | production
NODE_ENV=development

# Zona horaria operativa del negocio (Costa Rica).
TZ=America/Costa_Rica
